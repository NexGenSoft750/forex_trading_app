import { commonMetadata } from "@/lib/metadata";

export const metadata = commonMetadata.fxAnalyzerPro;

export default function FxAnalyzerProLayout({ children }: { children: React.ReactNode }) {
    return (
        <>
            {children}
        </>
    );
};